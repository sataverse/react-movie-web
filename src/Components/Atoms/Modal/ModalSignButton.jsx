import styled from 'styled-components'

const ModalSignButton = styled.button`
    width: 230rem;
    height: 40rem;
    border: 0;
    border-radius: 8px;
    background-color: var(--w-red);
    font-size: 14rem;
    color: var(--w-white);
    &:hover {
        cursor: pointer;
        background-color: #e90d0d;
    }
`

export default ModalSignButton
